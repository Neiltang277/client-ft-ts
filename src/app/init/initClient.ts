import appAction from './createAction';

const initClient = (dispatch: any) => {
  let storage = window.localStorage;
  const user  = storage.getItem('user')


  return [];
};

export default initClient;
